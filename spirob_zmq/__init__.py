"""ZeroMQ port of the ``ros/spirob_ros`` nodes.

Same nodes, same topics, same message fields as the ROS 2 package, but with
no ROS dependency, so it runs on Linux, macOS and Windows. See README.md.
"""
